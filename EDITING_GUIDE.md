# دليل تعديل الموقع

## الصور والمشاريع

كل بيانات معرض الأعمال موجودة في `data/projects.js`. لكل مشروع:

- `coverImage`: الصورة الأولى التي تظهر في بطاقة المعرض.
- `images`: باقي صور المشروع داخل النافذة.
- `videos`: فيديوهات المشروع، ويمكن أن تكون ملفات `mp4` داخل `images/projects/`.
- `title` و`description`: النص الظاهر مع المشروع.
- `location` و`year`: موقع المشروع وسنة التنفيذ.
- `area` و`materials` و`duration`: تفاصيل تظهر داخل نافذة المشروع.

لإضافة مشروع جديد، انسخ قالب المشروع الموجود داخل قائمة `projects`، املأ بياناته، وأضف فاصلة بعد المشروع السابق. لا تحتاج إلى استخدام لوحة إدارة أو قاعدة بيانات.

ضع صورك داخل `images/projects/` واستخدم مسارات مثل:
`images/projects/kitchen-01.jpg`

لإضافة فيديو استخدم مسارًا مثل:
`images/projects/kitchen-tour.mp4`

يظهر داخل كل مشروع زر واتساب برسالة تلقائية تذكر اسم المشروع للعميل.

## النصوص العامة

النصوص العامة مثل اسم الشركة والعناوين والأوصاف موجودة في `index.html` ويمكن تعديلها مباشرة مع الحفاظ على البنية والعناوين.

## التوافق

التصميم متجاوب مع الجوال والتابلت والكمبيوتر، ومعرض الصور يستخدم صورًا Responsive ويفتح داخل نافذة مناسبة للشاشات الصغيرة.

## تعديل المشاريع بدون قاعدة بيانات

الموقع يقرأ المشاريع مباشرة من `data/projects.js`. لتغيير صورة:

1. ضع الصورة داخل `images/projects/`.
2. افتح المشروع المطلوب في `data/projects.js`.
3. غيّر `coverImage` لصورة الغلاف، أو أضف المسار داخل `images` للصور الإضافية.
4. احفظ الملفات وارفعها إلى الاستضافة.

استخدم مسارًا مثل `images/projects/villa-palm-01.jpg`. ويمكنك إضافة مشروع جديد بنسخ أحد عناصر `projects` وتعديل بياناته. ستظهر كل المشاريع معًا بدون تصنيفات أو فلاتر.